interface:
  display_name: "SmartRoute"
  short_description: "Repo-specific patterns and workflows for SmartRoute"
  default_prompt: "Use the SmartRoute repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true